# Bale Game Bot — Frontend Environment Variables
# Copy this file to `.env` and set your server URL

# WebSocket URL (realtime server)
VITE_WS_URL=ws://localhost:8080/ws

# HTTP API URL (registration, leaderboard, online users)
VITE_API_URL=http://localhost:8080
